# ChimangoScan released dataset: the canonical paper-freeze artifacts.
# One record per line: <local_path>|<published_basename>|<role>
# publish_dataset.sh compresses (if needed), splits into <2GB parts, checksums,
# and uploads each to the GitHub release; fetch_dataset.sh reverses it.
# Paths are resolved relative to $DATASET_SRC (env, default this dir) unless absolute.
chimangoscan-reports.db.zst|chimangoscan-reports.db.zst|sqlite-scan-reports
dockerhub_data.freeze.archive.gz|dockerhub_data.freeze.archive.gz|mongodb-crawl
neo4j_data.freeze.tar.gz|neo4j_data.freeze.tar.gz|neo4j-layer-graph
exposure_work.freeze.tar|exposure_work.freeze.tar|layer-graph-dumps
